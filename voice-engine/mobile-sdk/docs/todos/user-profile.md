# TODO: 用户画像

## 目标

从长期记忆卡聚合出用户画像，供场景推荐、语速/风格默认值和播客个性化使用。

## 第一版范围

- 从本地记忆卡聚合画像，不让模型自由生成。
- 输出稳定事实、偏好、互动方式、待承接话题和边界。
- 画像结果保留证据来源的场景 ID 和更新时间。

## 验收

- 按 `userId` 可生成跨场景画像。
- 按 `sceneId` 可生成单场景画像。
- 没有记忆卡时返回空画像和警告。
- 有单元测试覆盖聚合逻辑。

## 后续

- 前端增加画像查看和编辑入口。
- 增加画像版本和更新时间排序。
- 用画像驱动默认场景推荐和播客风格。
